spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/job_posting
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
admin:
  username: admin
  password: password
jwt:
  key: key